version: "3.9"

services:
  cloudpulse:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cloudpulse-app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    env_file:
      - .env
    restart: unless-stopped
